# DomainBridge-Bengali-Automatic-Speech-Recognition
Built a top a fine-tuned Whisper encoder-decoder backbone, DOMAINBRIDGE is the first system to address Bengali out-of-distribution (OOD) degradation as a principled architectural objective rather than a datacollection artefact.
